# Copy to `.env` in the same directory as docker-compose.yml (not .env.local).
# Docker Compose reads `.env` automatically for variable substitution in this file.
#
# Use this when the host already runs nginx/apache on 80/443 (e.g. certbot standalone).
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
