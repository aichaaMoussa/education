version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: itekane-app
    restart: unless-stopped
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongo:27017/education
    volumes:
      - storage-data:/srv/itekane-storage
    networks:
      - itekane-network
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    container_name: itekane-mongo
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    networks:
      - itekane-network
    environment:
      - MONGO_INITDB_DATABASE=education

  nginx:
    image: nginx:alpine
    container_name: itekane-nginx
    restart: unless-stopped
    # Host ports: override via a root `.env` file (see compose.env.example) if system nginx/apache already use 80/443.
    ports:
      - "${NGINX_HTTP_PORT:-80}:80"
      - "${NGINX_HTTPS_PORT:-443}:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - storage-data:/srv/itekane-storage:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      # Let’s Encrypt HTTP-01: Certbot écrit ici, Nginx sert /.well-known/acme-challenge/
      - ./nginx/certbot:/var/www/certbot
    networks:
      - itekane-network
    depends_on:
      - app

volumes:
  storage-data:
    driver: local
  mongo-data:
    driver: local

networks:
  itekane-network:
    driver: bridge

